version: '3.8'

services:
  smart-music-assistant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smart-music-assistant
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
